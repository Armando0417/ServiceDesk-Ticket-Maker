# Legacy pre-3.6 files

This directory preserves the application and extension files that existed before the `3.6.2026.731` release layout.

They are retained for reference and rollback only. The maintained application lives at the repository root, and the maintained extensions live in `Edge_extension/` and `Edge_extension_alt/`.

Do not distribute the archived response or user-data files as part of a normal release package.
